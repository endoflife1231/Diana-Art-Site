interface:
  display_name: Browser Testing With Devtools
  short_description: Debug and verify web applications in a live Chrome browser through
    Chrome DevTools MCP. Use for DOM, console, network, s
  default_prompt: Use $browser-testing-with-devtools for this task. Follow its workflow,
    inspect the relevant project files, and verify the result.
dependencies:
  tools:
  - type: mcp
    value: chrome_devtools
    description: Chrome DevTools MCP server for live browser inspection
